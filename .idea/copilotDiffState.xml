<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/rush-say/src/section.rs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/rush-say/src/section.rs" />
              <option name="originalContent" value="use crate::border::{BorderStyle, BubbleStyle, CommentStyle};&#10;use crate::visual_width_char;&#10;&#10;#[derive(Debug, Clone, Default)]&#10;pub struct Section {&#10;    pub style: BubbleStyle,&#10;    pub comment_style: Option&lt;CommentStyle&gt;,&#10;}&#10;&#10;impl Section {&#10;    pub fn new(width: usize, padding: usize, border_style: BorderStyle, comment_style: Option&lt;CommentStyle&gt;) -&gt; Self {&#10;        Section {&#10;            style: BubbleStyle { width, padding, border_style },&#10;            comment_style,&#10;        }&#10;    }&#10;&#10;    pub fn builder() -&gt; SectionBuilder {&#10;        SectionBuilder::default()&#10;    }&#10;&#10;    /// 输出气泡内容，自动加注释前缀/后缀（如果有）&#10;    pub fn say(&amp;self, writer: &amp;mut impl std::io::Write, content: impl AsRef&lt;str&gt;) -&gt; std::io::Result&lt;()&gt; {&#10;        let max_line_width = self.style.width - 2 - self.style.padding * 2;&#10;        let top_border = format!(&#10;            &quot;{}{}{}&quot;,&#10;            self.style.border_style.top_left,&#10;            self.style.border_style.horizontal.to_string().repeat(self.style.width - 2),&#10;            self.style.border_style.top_right&#10;        );&#10;        writeln!(writer, &quot;{top_border}&quot;)?;&#10;&#10;        let mut chars = content.as_ref().chars().peekable();&#10;        loop {&#10;            while chars.peek().is_some() &amp;&amp; *chars.peek().unwrap() == '\n' {&#10;                write!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;                for _ in 0..(self.style.width - 2) {&#10;                    write!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;                }&#10;                writeln!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;                chars.next();&#10;            }&#10;            if chars.peek().is_none() {&#10;                break;&#10;            }&#10;            let mut current = String::new();&#10;            let mut visual = 0;&#10;            while let Some(&amp;ch) = chars.peek() {&#10;                if ch == '\n' {&#10;                    break;&#10;                }&#10;                let ch_width = visual_width_char(ch);&#10;                if visual + ch_width &gt; max_line_width {&#10;                    break;&#10;                }&#10;                visual += ch_width;&#10;                current.push(ch);&#10;                chars.next();&#10;            }&#10;            if chars.peek() == Some(&amp;'\n') {&#10;                chars.next();&#10;            }&#10;            let spaces = max_line_width - visual;&#10;            let left = self.style.padding + spaces / 2;&#10;            let right = self.style.padding + (spaces - spaces / 2);&#10;            write!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;            for _ in 0..left {&#10;                write!(writer, &quot; &quot;)?;&#10;            }&#10;            write!(writer, &quot;{current}&quot;)?;&#10;            for _ in 0..right {&#10;                write!(writer, &quot; &quot;)?;&#10;            }&#10;            writeln!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;        }&#10;        let bottom_border = format!(&#10;            &quot;{}{}{}&quot;,&#10;            self.style.border_style.bottom_left,&#10;            self.style.border_style.horizontal.to_string().repeat(self.style.width - 2),&#10;            self.style.border_style.bottom_right&#10;        );&#10;        writeln!(writer, &quot;{bottom_border}&quot;)?;&#10;        Ok(())&#10;    }&#10;&#10;    pub fn style(&amp;self) -&gt; &amp;BubbleStyle {&#10;        &amp;self.style&#10;    }&#10;    pub fn comment_style(&amp;self) -&gt; Option&lt;&amp;CommentStyle&gt; {&#10;        self.comment_style.as_ref()&#10;    }&#10;}&#10;&#10;#[derive(Debug, Clone)]&#10;pub struct SectionBuilder {&#10;    width: usize,&#10;    padding: usize,&#10;    border_style: BorderStyle,&#10;    comment_style: Option&lt;CommentStyle&gt;,&#10;}&#10;&#10;impl Default for SectionBuilder {&#10;    fn default() -&gt; Self {&#10;        SectionBuilder {&#10;            width: 48,&#10;            padding: 2,&#10;            border_style: BorderStyle::default(),&#10;            comment_style: None,&#10;        }&#10;    }&#10;}&#10;&#10;impl SectionBuilder {&#10;    pub fn width(mut self, width: usize) -&gt; Self {&#10;        self.width = width;&#10;        self&#10;    }&#10;    pub fn padding(mut self, padding: usize) -&gt; Self {&#10;        self.padding = padding;&#10;        self&#10;    }&#10;    pub fn border_style(mut self, border_style: BorderStyle) -&gt; Self {&#10;        self.border_style = border_style;&#10;        self&#10;    }&#10;    pub fn comment_style(mut self, comment_style: CommentStyle) -&gt; Self {&#10;        self.comment_style = Some(comment_style);&#10;        self&#10;    }&#10;    pub fn build(self) -&gt; Section {&#10;        Section {&#10;            style: BubbleStyle {&#10;                width: self.width,&#10;                padding: self.padding,&#10;                border_style: self.border_style,&#10;            },&#10;            comment_style: self.comment_style,&#10;        }&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="use crate::border::{BorderStyle, BubbleStyle, CommentStyle};&#10;use crate::visual_width_char;&#10;&#10;#[derive(Debug, Clone, Default)]&#10;pub struct Section {&#10;    pub style: BubbleStyle,&#10;    pub comment_style: Option&lt;CommentStyle&gt;,&#10;}&#10;&#10;impl Section {&#10;    pub fn new(width: usize, padding: usize, border_style: BorderStyle, comment_style: Option&lt;CommentStyle&gt;) -&gt; Self {&#10;        Section {&#10;            style: BubbleStyle { width, padding, border_style },&#10;            comment_style,&#10;        }&#10;    }&#10;&#10;    pub fn builder() -&gt; SectionBuilder {&#10;        SectionBuilder::default()&#10;    }&#10;&#10;    /// 输出气泡内容，自动加注释前缀/后缀（如果有）&#10;    pub fn say(&amp;self, writer: &amp;mut impl std::io::Write, content: impl AsRef&lt;str&gt;) -&gt; std::io::Result&lt;()&gt; {&#10;        let max_line_width = self.style.width - 2 - self.style.padding * 2;&#10;        let top_border = format!(&#10;            &quot;{}{}{}&quot;,&#10;            self.style.border_style.top_left,&#10;            self.style.border_style.horizontal.to_string().repeat(self.style.width - 2),&#10;            self.style.border_style.top_right&#10;        );&#10;        writeln!(writer, &quot;{top_border}&quot;)?;&#10;&#10;        let mut chars = content.as_ref().chars().peekable();&#10;        loop {&#10;            while chars.peek().is_some() &amp;&amp; *chars.peek().unwrap() == '\n' {&#10;                write!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;                for _ in 0..(self.style.width - 2) {&#10;                    write!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;                }&#10;                writeln!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;                chars.next();&#10;            }&#10;            if chars.peek().is_none() {&#10;                break;&#10;            }&#10;            let mut current = String::new();&#10;            let mut visual = 0;&#10;            while let Some(&amp;ch) = chars.peek() {&#10;                if ch == '\n' {&#10;                    break;&#10;                }&#10;                let ch_width = visual_width_char(ch);&#10;                if visual + ch_width &gt; max_line_width {&#10;                    break;&#10;                }&#10;                visual += ch_width;&#10;                current.push(ch);&#10;                chars.next();&#10;            }&#10;            if chars.peek() == Some(&amp;'\n') {&#10;                chars.next();&#10;            }&#10;            let spaces = max_line_width - visual;&#10;            let left = self.style.padding + spaces / 2;&#10;            let right = self.style.padding + (spaces - spaces / 2);&#10;            write!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;            for _ in 0..left {&#10;                write!(writer, &quot; &quot;)?;&#10;            }&#10;            write!(writer, &quot;{current}&quot;)?;&#10;            for _ in 0..right {&#10;                write!(writer, &quot; &quot;)?;&#10;            }&#10;            writeln!(writer, &quot;{}&quot;, self.style.border_style.vertical)?;&#10;        }&#10;        let bottom_border = format!(&#10;            &quot;{}{}{}&quot;,&#10;            self.style.border_style.bottom_left,&#10;            self.style.border_style.horizontal.to_string().repeat(self.style.width - 2),&#10;            self.style.border_style.bottom_right&#10;        );&#10;        writeln!(writer, &quot;{bottom_border}&quot;)?;&#10;        Ok(())&#10;    }&#10;&#10;    pub fn style(&amp;self) -&gt; &amp;BubbleStyle {&#10;        &amp;self.style&#10;    }&#10;    pub fn comment_style(&amp;self) -&gt; Option&lt;&amp;CommentStyle&gt; {&#10;        self.comment_style.as_ref()&#10;    }&#10;}&#10;&#10;#[derive(Debug, Clone)]&#10;pub struct SectionBuilder {&#10;    width: usize,&#10;    padding: usize,&#10;    border_style: BorderStyle,&#10;    comment_style: Option&lt;CommentStyle&gt;,&#10;}&#10;&#10;impl Default for SectionBuilder {&#10;    fn default() -&gt; Self {&#10;        SectionBuilder {&#10;            width: 48,&#10;            padding: 2,&#10;            border_style: BorderStyle::default(),&#10;            comment_style: None,&#10;        }&#10;    }&#10;}&#10;&#10;impl SectionBuilder {&#10;    pub fn width(mut self, width: usize) -&gt; Self {&#10;        self.width = width;&#10;        self&#10;    }&#10;    pub fn padding(mut self, padding: usize) -&gt; Self {&#10;        self.padding = padding;&#10;        self&#10;    }&#10;    pub fn border_style(mut self, border_style: BorderStyle) -&gt; Self {&#10;        self.border_style = border_style;&#10;        self&#10;    }&#10;    pub fn comment_style(mut self, comment_style: CommentStyle) -&gt; Self {&#10;        self.comment_style = Some(comment_style);&#10;        self&#10;    }&#10;    pub fn build(self) -&gt; Section {&#10;        Section {&#10;            style: BubbleStyle {&#10;                width: self.width,&#10;                padding: self.padding,&#10;                border_style: self.border_style,&#10;            },&#10;            comment_style: self.comment_style,&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/rush-say/src/str_ext.rs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/rush-say/src/str_ext.rs" />
              <option name="originalContent" value="use crate::widget::visual_width_char;&#10;use std::ops::Range;&#10;&#10;pub trait StrExt {&#10;    fn visual_width(&amp;self) -&gt; usize;&#10;&#10;    fn lines_with_range(&amp;self) -&gt; Vec&lt;(&amp;str, Range&lt;usize&gt;)&gt;;&#10;}&#10;&#10;impl StrExt for str {&#10;    fn visual_width(&amp;self) -&gt; usize {&#10;        self.chars().map(visual_width_char).sum()&#10;    }&#10;&#10;    fn lines_with_range(&amp;self) -&gt; Vec&lt;(&amp;str, Range&lt;usize&gt;)&gt; {&#10;        let bytes = self.as_bytes();&#10;        let mut lines = Vec::new();&#10;&#10;        let mut start = 0;&#10;        let mut i = 0;&#10;&#10;        while i &lt; bytes.len() {&#10;            match bytes[i] {&#10;                b'\n' =&gt; {&#10;                    // handle '\n'&#10;                    let range = start..i;&#10;                    lines.push((&amp;self[range.clone()], range));&#10;                    i += 1;&#10;                    start = i;&#10;                }&#10;                b'\r' =&gt; {&#10;                    if i + 1 &lt; bytes.len() &amp;&amp; bytes[i + 1] == b'\n' {&#10;                        // handle '\r\n'&#10;                        let range = start..i;&#10;                        lines.push((&amp;self[range.clone()], range));&#10;                        i += 2;&#10;                    } else {&#10;                        // handle lone '\r'&#10;                        let range = start..i;&#10;                        lines.push((&amp;self[range.clone()], range));&#10;                        i += 1;&#10;                    }&#10;                    start = i;&#10;                }&#10;                _ =&gt; {&#10;                    i += 1;&#10;                }&#10;            }&#10;        }&#10;&#10;        // 最后一行（可能没有换行符）&#10;        if start &lt; self.len() {&#10;            let range = start..self.len();&#10;            lines.push((&amp;self[range.clone()], range));&#10;        }&#10;&#10;        lines&#10;    }&#10;}&#10;&#10;#[cfg(test)]&#10;mod tests {&#10;    use super::*;&#10;    use crate::widget::visual_width_char;&#10;&#10;    #[test]&#10;    fn test_visual_width_ascii() {&#10;        assert_eq!(&quot;hello&quot;.visual_width(), 5);&#10;        assert_eq!(&quot;&quot;.visual_width(), 0);&#10;    }&#10;&#10;    #[test]&#10;    fn test_visual_width_unicode() {&#10;        // 假设 visual_width_char('你') == 2&#10;        let width_ni = visual_width_char('你');&#10;        let width_hao = visual_width_char('好');&#10;        assert_eq!(&quot;你&quot;.visual_width(), width_ni);&#10;        assert_eq!(&quot;你好&quot;.visual_width(), width_ni + width_hao);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_basic() {&#10;        let s = &quot;hello\nworld&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 2);&#10;        assert_eq!(lines[0].0, &quot;hello&quot;);&#10;        assert_eq!(lines[0].1, 0..5);&#10;        assert_eq!(lines[1].0, &quot;world&quot;);&#10;        assert_eq!(lines[1].1, 6..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_crlf() {&#10;        let s = &quot;foo\r\nbar\r\nbaz&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 3);&#10;        assert_eq!(lines[0].0, &quot;foo&quot;);&#10;        assert_eq!(lines[0].1, 0..3);&#10;        assert_eq!(lines[1].0, &quot;bar&quot;);&#10;        assert_eq!(lines[1].1, 4..7);&#10;        assert_eq!(lines[2].0, &quot;baz&quot;);&#10;        assert_eq!(lines[2].1, 8..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_lone_cr() {&#10;        let s = &quot;a\rb\rc&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 3);&#10;        assert_eq!(lines[0].0, &quot;a&quot;);&#10;        assert_eq!(lines[0].1, 0..1);&#10;        assert_eq!(lines[1].0, &quot;b&quot;);&#10;        assert_eq!(lines[1].1, 2..3);&#10;        assert_eq!(lines[2].0, &quot;c&quot;);&#10;        assert_eq!(lines[2].1, 4..5);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_trailing_newline() {&#10;        let s = &quot;foo\n&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 1);&#10;        assert_eq!(lines[0].0, &quot;foo&quot;);&#10;        assert_eq!(lines[0].1, 0..3);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_empty() {&#10;        let s = &quot;&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert!(lines.is_empty());&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_spaces_only() {&#10;        let s = &quot;   &quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 1);&#10;        assert_eq!(lines[0].0, &quot;   &quot;);&#10;        assert_eq!(lines[0].1, 0..3);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_leading_spaces() {&#10;        let s = &quot;  foo\n  bar&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 2);&#10;        assert_eq!(lines[0].0, &quot;  foo&quot;);&#10;        assert_eq!(lines[0].1, 0..5);&#10;        assert_eq!(lines[1].0, &quot;  bar&quot;);&#10;        assert_eq!(lines[1].1, 6..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_trailing_spaces() {&#10;        let s = &quot;foo  \nbar  &quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 2);&#10;        assert_eq!(lines[0].0, &quot;foo  &quot;);&#10;        assert_eq!(lines[0].1, 0..5);&#10;        assert_eq!(lines[1].0, &quot;bar  &quot;);&#10;        assert_eq!(lines[1].1, 6..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_crlf_with_spaces() {&#10;        let s = &quot;foo \r\n bar\r\n  baz &quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 3);&#10;        assert_eq!(lines[0].0, &quot;foo &quot;);&#10;        assert_eq!(lines[0].1, 0..4);&#10;        assert_eq!(lines[1].0, &quot; bar&quot;);&#10;        assert_eq!(lines[1].1, 5..9);&#10;        assert_eq!(lines[2].0, &quot;  baz &quot;);&#10;        assert_eq!(lines[2].1, 10..16);&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="use crate::widget::visual_width_char;&#10;use std::ops::Range;&#10;&#10;pub trait StrExt {&#10;    fn visual_width(&amp;self) -&gt; usize;&#10;&#10;    fn lines_with_range(&amp;self) -&gt; Vec&lt;(&amp;str, Range&lt;usize&gt;)&gt;;&#10;}&#10;&#10;impl StrExt for str {&#10;    fn visual_width(&amp;self) -&gt; usize {&#10;        self.chars().map(visual_width_char).sum()&#10;    }&#10;&#10;    fn lines_with_range(&amp;self) -&gt; Vec&lt;(&amp;str, Range&lt;usize&gt;)&gt; {&#10;        let bytes = self.as_bytes();&#10;        let mut lines = Vec::new();&#10;&#10;        let mut start = 0;&#10;        let mut i = 0;&#10;&#10;        while i &lt; bytes.len() {&#10;            match bytes[i] {&#10;                b'\n' =&gt; {&#10;                    // handle '\n'&#10;                    let range = start..i;&#10;                    lines.push((&amp;self[range.clone()], range));&#10;                    i += 1;&#10;                    start = i;&#10;                }&#10;                b'\r' =&gt; {&#10;                    if i + 1 &lt; bytes.len() &amp;&amp; bytes[i + 1] == b'\n' {&#10;                        // handle '\r\n'&#10;                        let range = start..i;&#10;                        lines.push((&amp;self[range.clone()], range));&#10;                        i += 2;&#10;                    } else {&#10;                        // handle lone '\r'&#10;                        let range = start..i;&#10;                        lines.push((&amp;self[range.clone()], range));&#10;                        i += 1;&#10;                    }&#10;                    start = i;&#10;                }&#10;                _ =&gt; {&#10;                    i += 1;&#10;                }&#10;            }&#10;        }&#10;&#10;        // 最后一行（可能没有换行符）&#10;        if start &lt; self.len() {&#10;            let range = start..self.len();&#10;            lines.push((&amp;self[range.clone()], range));&#10;        }&#10;&#10;        lines&#10;    }&#10;}&#10;&#10;#[cfg(test)]&#10;mod tests {&#10;    use super::*;&#10;    use crate::widget::visual_width_char;&#10;&#10;    #[test]&#10;    fn test_visual_width_ascii() {&#10;        assert_eq!(&quot;hello&quot;.visual_width(), 5);&#10;        assert_eq!(&quot;&quot;.visual_width(), 0);&#10;    }&#10;&#10;    #[test]&#10;    fn test_visual_width_unicode() {&#10;        // 假设 visual_width_char('你') == 2&#10;        let width_ni = visual_width_char('你');&#10;        let width_hao = visual_width_char('好');&#10;        assert_eq!(&quot;你&quot;.visual_width(), width_ni);&#10;        assert_eq!(&quot;你好&quot;.visual_width(), width_ni + width_hao);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_basic() {&#10;        let s = &quot;hello\nworld&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 2);&#10;        assert_eq!(lines[0].0, &quot;hello&quot;);&#10;        assert_eq!(lines[0].1, 0..5);&#10;        assert_eq!(lines[1].0, &quot;world&quot;);&#10;        assert_eq!(lines[1].1, 6..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_crlf() {&#10;        let s = &quot;foo\r\nbar\r\nbaz&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 3);&#10;        assert_eq!(lines[0].0, &quot;foo&quot;);&#10;        assert_eq!(lines[0].1, 0..3);&#10;        assert_eq!(lines[1].0, &quot;bar&quot;);&#10;        assert_eq!(lines[1].1, 5..8);&#10;        assert_eq!(lines[2].0, &quot;baz&quot;);&#10;        assert_eq!(lines[2].1, 10..13);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_lone_cr() {&#10;        let s = &quot;a\rb\rc&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 3);&#10;        assert_eq!(lines[0].0, &quot;a&quot;);&#10;        assert_eq!(lines[0].1, 0..1);&#10;        assert_eq!(lines[1].0, &quot;b&quot;);&#10;        assert_eq!(lines[1].1, 2..3);&#10;        assert_eq!(lines[2].0, &quot;c&quot;);&#10;        assert_eq!(lines[2].1, 4..5);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_trailing_newline() {&#10;        let s = &quot;foo\n&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 1);&#10;        assert_eq!(lines[0].0, &quot;foo&quot;);&#10;        assert_eq!(lines[0].1, 0..3);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_empty() {&#10;        let s = &quot;&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert!(lines.is_empty());&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_spaces_only() {&#10;        let s = &quot;   &quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 1);&#10;        assert_eq!(lines[0].0, &quot;   &quot;);&#10;        assert_eq!(lines[0].1, 0..3);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_leading_spaces() {&#10;        let s = &quot;  foo\n  bar&quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 2);&#10;        assert_eq!(lines[0].0, &quot;  foo&quot;);&#10;        assert_eq!(lines[0].1, 0..5);&#10;        assert_eq!(lines[1].0, &quot;  bar&quot;);&#10;        assert_eq!(lines[1].1, 6..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_trailing_spaces() {&#10;        let s = &quot;foo  \nbar  &quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 2);&#10;        assert_eq!(lines[0].0, &quot;foo  &quot;);&#10;        assert_eq!(lines[0].1, 0..5);&#10;        assert_eq!(lines[1].0, &quot;bar  &quot;);&#10;        assert_eq!(lines[1].1, 6..11);&#10;    }&#10;&#10;    #[test]&#10;    fn test_lines_with_range_crlf_with_spaces() {&#10;        let s = &quot;foo \r\n bar\r\n  baz &quot;;&#10;        let lines = s.lines_with_range();&#10;        assert_eq!(lines.len(), 3);&#10;        assert_eq!(lines[0].0, &quot;foo &quot;);&#10;        assert_eq!(lines[0].1, 0..4);&#10;        assert_eq!(lines[1].0, &quot; bar&quot;);&#10;        assert_eq!(lines[1].1, 6..10);&#10;        assert_eq!(lines[2].0, &quot;  baz &quot;);&#10;        assert_eq!(lines[2].1, 12..18);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/rush-say/src/widget/text.rs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/rush-say/src/widget/text.rs" />
              <option name="originalContent" value="use crate::widget::{Align, Constraints, Size, Widget};&#10;use std::io::Write;&#10;use std::ops::Range;&#10;use unicode_segmentation::UnicodeSegmentation;&#10;use unicode_width::UnicodeWidthStr;&#10;&#10;pub struct Text {&#10;    pub content: String,&#10;    pub graphemes: Vec&lt;TextGrapheme&gt;,&#10;    pub align: Align,&#10;}&#10;&#10;#[derive(Debug, Clone)]&#10;pub struct TextGrapheme {&#10;    pub range: Range&lt;usize&gt;,&#10;    pub width: usize,&#10;}&#10;&#10;#[derive(Default, Debug, Clone)]&#10;pub struct TextLine {&#10;    pub graphemes: Vec&lt;TextGrapheme&gt;,&#10;    pub width: usize,&#10;}&#10;&#10;impl Text {&#10;    pub fn new(content: impl AsRef&lt;str&gt;) -&gt; Text {&#10;        Self::with_align(content, Align::Center)&#10;    }&#10;&#10;    pub fn with_align(content: impl AsRef&lt;str&gt;, align: Align) -&gt; Text {&#10;        let content = content.as_ref().to_string();&#10;        let grapheme_indices = content.grapheme_indices(true);&#10;        let graphemes = grapheme_indices&#10;            .map(|(start, grapheme)| {&#10;                let range = start..start + grapheme.len();&#10;                let width = grapheme.width();&#10;                TextGrapheme { range, width }&#10;            })&#10;            .collect::&lt;Vec&lt;_&gt;&gt;();&#10;        Text { content, graphemes, align }&#10;    }&#10;&#10;    pub fn set_align(mut self, align: Align) -&gt; Self {&#10;        self.align = align;&#10;        self&#10;    }&#10;&#10;    pub fn wrap_text(&amp;mut self, max_width: usize) -&gt; Vec&lt;TextLine&gt; {&#10;        let mut lines: Vec&lt;TextLine&gt; = vec![];&#10;        let mut current_line: TextLine = TextLine::default();&#10;        for grapheme in std::mem::take(&amp;mut self.graphemes) {&#10;            let grapheme_str = &amp;self.content[grapheme.range.clone()];&#10;            println!(&quot;{grapheme_str:?}&quot;);&#10;            match grapheme_str {&#10;                &quot;\n&quot; | &quot;\r&quot; | &quot;\r\n&quot; =&gt; {&#10;                    lines.push(std::mem::take(&amp;mut current_line));&#10;                }&#10;                _ =&gt; {&#10;                    if current_line.width + grapheme.width &gt; max_width {&#10;                        // 如果当前行宽度超过限制，则换行&#10;                        lines.push(std::mem::take(&amp;mut current_line));&#10;                    }&#10;                    current_line.push(grapheme);&#10;                }&#10;            }&#10;        }&#10;        lines.push(current_line);&#10;        lines&#10;    }&#10;}&#10;&#10;impl Widget for Text {&#10;    fn layout(&amp;mut self, constraints: Constraints) -&gt; Size {&#10;        let lines = self.wrap_text(constraints.max_width);&#10;        todo!()&#10;    }&#10;&#10;    fn render(&amp;self, writer: &amp;mut impl Write, row: usize) -&gt; std::io::Result&lt;()&gt; {&#10;        todo!()&#10;    }&#10;}&#10;&#10;impl TextGrapheme {&#10;    pub fn render&lt;'a&gt;(&amp;'a self, content: &amp;'a str) -&gt; &amp;'a str {&#10;        &amp;content[self.range.clone()]&#10;    }&#10;}&#10;&#10;impl TextLine {&#10;    pub fn push(&amp;mut self, grapheme: TextGrapheme) {&#10;        self.width += grapheme.width;&#10;        self.graphemes.push(grapheme);&#10;    }&#10;&#10;    pub fn render&lt;'a&gt;(&amp;'a self, content: &amp;'a str) -&gt; &amp;'a str {&#10;        let range = match (self.graphemes.first(), self.graphemes.last()) {&#10;            (Some(first), Some(last)) =&gt; first.range.start..last.range.end,&#10;            _ =&gt; 0..0, // 如果没有字符，则返回空范围&#10;        };&#10;        &amp;content[range]&#10;    }&#10;}&#10;&#10;#[cfg(test)]&#10;mod tests {&#10;    use crate::widget::text::Text;&#10;    use crate::widget::{Constraints, Widget};&#10;&#10;    #[test]&#10;    fn test_text_widget() {&#10;        let mut text = Text::new(&quot;Hel‍lo,\r\nworld!&quot;);&#10;        let lines = text.wrap_text(4);&#10;        assert_eq!(5, lines.len());&#10;&#10;        assert_eq!(3, lines[0].width);&#10;        assert_eq!(&quot;Hel&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;‍lo&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;,&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[3].width);&#10;        assert_eq!(&quot;worl&quot;, lines[3].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[4].width);&#10;        assert_eq!(&quot;d!&quot;, lines[4].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_simple_ascii() {&#10;        let mut text = Text::new(&quot;hello world&quot;);&#10;        let lines = text.wrap_text(5);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot; worl&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;d&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_exact_fit() {&#10;        let mut text = Text::new(&quot;hello&quot;);&#10;        let lines = text.wrap_text(5);&#10;        assert_eq!(1, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_single_char_per_line() {&#10;        let mut text = Text::new(&quot;abc&quot;);&#10;        let lines = text.wrap_text(1);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(1, lines[0].width);&#10;        assert_eq!(&quot;a&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[1].width);&#10;        assert_eq!(&quot;b&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;c&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_empty_string() {&#10;        let mut text = Text::new(&quot;&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(1, lines.len());&#10;&#10;        assert_eq!(0, lines[0].width);&#10;        assert_eq!(&quot;&quot;, lines[0].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_newline_only() {&#10;        let mut text = Text::new(&quot;hello\nworld&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot;world&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_multiple_newlines() {&#10;        let mut text = Text::new(&quot;a\n\nb&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(1, lines[0].width);&#10;        assert_eq!(&quot;a&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(0, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;b&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_unicode_wide_chars() {&#10;        let mut text = Text::new(&quot;你好世界&quot;);&#10;        let lines = text.wrap_text(4);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(4, lines[0].width);&#10;        assert_eq!(&quot;你好&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;世界&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_mixed_ascii_unicode() {&#10;        let mut text = Text::new(&quot;hello你好&quot;);&#10;        let lines = text.wrap_text(6);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;你好&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_cr_line_ending() {&#10;        let mut text = Text::new(&quot;line1\rline2&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;line1&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot;line2&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_trailing_newline() {&#10;        let mut text = Text::new(&quot;hello\n&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(0, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_very_long_word() {&#10;        let mut text = Text::new(&quot;supercalifragilisticexpialidocious&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(4, lines.len());&#10;&#10;        assert_eq!(10, lines[0].width);&#10;        assert_eq!(&quot;supercalif&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(10, lines[1].width);&#10;        assert_eq!(&quot;ragilistic&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(10, lines[2].width);&#10;        assert_eq!(&quot;expialidoc&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[3].width);&#10;        assert_eq!(&quot;ious&quot;, lines[3].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_spaces_and_wrapping() {&#10;        let mut text = Text::new(&quot;a  b  c&quot;);&#10;        let lines = text.wrap_text(3);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(3, lines[0].width);&#10;        assert_eq!(&quot;a  &quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(3, lines[1].width);&#10;        assert_eq!(&quot;b  &quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;c&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="use crate::widget::{Align, Constraints, Size, Widget};&#10;use std::io::Write;&#10;use std::ops::Range;&#10;use unicode_segmentation::UnicodeSegmentation;&#10;use unicode_width::UnicodeWidthStr;&#10;&#10;pub struct Text {&#10;    pub content: String,&#10;    pub graphemes: Vec&lt;TextGrapheme&gt;,&#10;    pub align: Align,&#10;}&#10;&#10;#[derive(Debug, Clone)]&#10;pub struct TextGrapheme {&#10;    pub range: Range&lt;usize&gt;,&#10;    pub width: usize,&#10;}&#10;&#10;#[derive(Default, Debug, Clone)]&#10;pub struct TextLine {&#10;    pub graphemes: Vec&lt;TextGrapheme&gt;,&#10;    pub width: usize,&#10;}&#10;&#10;impl Text {&#10;    pub fn new(content: impl AsRef&lt;str&gt;) -&gt; Text {&#10;        Self::with_align(content, Align::Center)&#10;    }&#10;&#10;    pub fn with_align(content: impl AsRef&lt;str&gt;, align: Align) -&gt; Text {&#10;        let content = content.as_ref().to_string();&#10;        let grapheme_indices = content.grapheme_indices(true);&#10;        let graphemes = grapheme_indices&#10;            .map(|(start, grapheme)| {&#10;                let range = start..start + grapheme.len();&#10;                let width = grapheme.width();&#10;                TextGrapheme { range, width }&#10;            })&#10;            .collect::&lt;Vec&lt;_&gt;&gt;();&#10;        Text { content, graphemes, align }&#10;    }&#10;&#10;    pub fn set_align(mut self, align: Align) -&gt; Self {&#10;        self.align = align;&#10;        self&#10;    }&#10;&#10;    pub fn wrap_text(&amp;mut self, max_width: usize) -&gt; Vec&lt;TextLine&gt; {&#10;        let mut lines: Vec&lt;TextLine&gt; = vec![];&#10;        let mut current_line: TextLine = TextLine::default();&#10;        for grapheme in std::mem::take(&amp;mut self.graphemes) {&#10;            let grapheme_str = &amp;self.content[grapheme.range.clone()];&#10;            println!(&quot;{grapheme_str:?}&quot;);&#10;            match grapheme_str {&#10;                &quot;\n&quot; | &quot;\r&quot; | &quot;\r\n&quot; =&gt; {&#10;                    lines.push(std::mem::take(&amp;mut current_line));&#10;                }&#10;                _ =&gt; {&#10;                    if current_line.width + grapheme.width &gt; max_width {&#10;                        // 如果当前行宽度超过限制，则换行&#10;                        lines.push(std::mem::take(&amp;mut current_line));&#10;                    }&#10;                    current_line.push(grapheme);&#10;                }&#10;            }&#10;        }&#10;        lines.push(current_line);&#10;        lines&#10;    }&#10;}&#10;&#10;impl Widget for Text {&#10;    fn layout(&amp;mut self, constraints: Constraints) -&gt; Size {&#10;        let lines = self.wrap_text(constraints.max_width);&#10;        todo!()&#10;    }&#10;&#10;    fn render(&amp;self, writer: &amp;mut impl Write, row: usize) -&gt; std::io::Result&lt;()&gt; {&#10;        todo!()&#10;    }&#10;}&#10;&#10;impl TextGrapheme {&#10;    pub fn render&lt;'a&gt;(&amp;'a self, content: &amp;'a str) -&gt; &amp;'a str {&#10;        &amp;content[self.range.clone()]&#10;    }&#10;}&#10;&#10;impl TextLine {&#10;    pub fn push(&amp;mut self, grapheme: TextGrapheme) {&#10;        self.width += grapheme.width;&#10;        self.graphemes.push(grapheme);&#10;    }&#10;&#10;    pub fn render&lt;'a&gt;(&amp;'a self, content: &amp;'a str) -&gt; &amp;'a str {&#10;        let range = match (self.graphemes.first(), self.graphemes.last()) {&#10;            (Some(first), Some(last)) =&gt; first.range.start..last.range.end,&#10;            _ =&gt; 0..0, // 如果没有字符，则返回空范围&#10;        };&#10;        &amp;content[range]&#10;    }&#10;}&#10;&#10;#[cfg(test)]&#10;mod tests {&#10;    use crate::widget::text::Text;&#10;    use crate::widget::{Constraints, Widget};&#10;&#10;    #[test]&#10;    fn test_text_widget() {&#10;        let mut text = Text::new(&quot;Hel‍lo,\r\nworld!&quot;);&#10;        let lines = text.wrap_text(4);&#10;        assert_eq!(5, lines.len());&#10;&#10;        assert_eq!(3, lines[0].width);&#10;        assert_eq!(&quot;Hel&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;‍lo&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;,&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[3].width);&#10;        assert_eq!(&quot;worl&quot;, lines[3].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[4].width);&#10;        assert_eq!(&quot;d!&quot;, lines[4].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_simple_ascii() {&#10;        let mut text = Text::new(&quot;hello world&quot;);&#10;        let lines = text.wrap_text(5);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot; worl&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;d&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_exact_fit() {&#10;        let mut text = Text::new(&quot;hello&quot;);&#10;        let lines = text.wrap_text(5);&#10;        assert_eq!(1, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_single_char_per_line() {&#10;        let mut text = Text::new(&quot;abc&quot;);&#10;        let lines = text.wrap_text(1);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(1, lines[0].width);&#10;        assert_eq!(&quot;a&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[1].width);&#10;        assert_eq!(&quot;b&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;c&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_empty_string() {&#10;        let mut text = Text::new(&quot;&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(1, lines.len());&#10;&#10;        assert_eq!(0, lines[0].width);&#10;        assert_eq!(&quot;&quot;, lines[0].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_newline_only() {&#10;        let mut text = Text::new(&quot;hello\nworld&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot;world&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_multiple_newlines() {&#10;        let mut text = Text::new(&quot;a\n\nb&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(1, lines[0].width);&#10;        assert_eq!(&quot;a&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(0, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;b&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_unicode_wide_chars() {&#10;        let mut text = Text::new(&quot;你好世界&quot;);&#10;        let lines = text.wrap_text(4);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(4, lines[0].width);&#10;        assert_eq!(&quot;你好&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;世界&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_mixed_ascii_unicode() {&#10;        let mut text = Text::new(&quot;hello你好&quot;);&#10;        let lines = text.wrap_text(6);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;你好&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_cr_line_ending() {&#10;        let mut text = Text::new(&quot;line1\rline2&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;line1&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot;line2&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_trailing_newline() {&#10;        let mut text = Text::new(&quot;hello\n&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(0, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_very_long_word() {&#10;        let mut text = Text::new(&quot;supercalifragilisticexpialidocious&quot;);&#10;        let lines = text.wrap_text(10);&#10;        assert_eq!(4, lines.len());&#10;&#10;        assert_eq!(10, lines[0].width);&#10;        assert_eq!(&quot;supercalif&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(10, lines[1].width);&#10;        assert_eq!(&quot;ragilistic&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(10, lines[2].width);&#10;        assert_eq!(&quot;expialidoc&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[3].width);&#10;        assert_eq!(&quot;ious&quot;, lines[3].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_spaces_and_wrapping() {&#10;        let mut text = Text::new(&quot;a  b  c&quot;);&#10;        let lines = text.wrap_text(3);&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(3, lines[0].width);&#10;        assert_eq!(&quot;a  &quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(3, lines[1].width);&#10;        assert_eq!(&quot;b  &quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(1, lines[2].width);&#10;        assert_eq!(&quot;c&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_emoji_variant_narrow() {&#10;        let mut text = Text::new(&quot;Hel‍lo,wo‍rld!&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 3 });&#10;        assert_eq!(6, lines.len());&#10;&#10;        assert_eq!(3, lines[0].width);&#10;        assert_eq!(&quot;Hel&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[1].width);&#10;        assert_eq!(&quot;‍&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(3, lines[2].width);&#10;        assert_eq!(&quot;lo,&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[3].width);&#10;        assert_eq!(&quot;wo&quot;, lines[3].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[4].width);&#10;        assert_eq!(&quot;‍&quot;, lines[4].render(&amp;text.content));&#10;&#10;        assert_eq!(3, lines[5].width);&#10;        assert_eq!(&quot;rld!&quot;, lines[5].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_emoji_variant_wide() {&#10;        let mut text = Text::new(&quot;Hel‍lo,wo‍rld!&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 10 });&#10;        assert_eq!(2, lines.len());&#10;&#10;        assert_eq!(8, lines[0].width);&#10;        assert_eq!(&quot;Hel‍lo,&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(7, lines[1].width);&#10;        assert_eq!(&quot;wo‍rld!&quot;, lines[1].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_mixed_emoji_newlines() {&#10;        let mut text = Text::new(&quot;Hello\nWorld\r\nEnd!&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 8 });&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(7, lines[0].width);&#10;        assert_eq!(&quot;Hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(7, lines[1].width);&#10;        assert_eq!(&quot;World&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[2].width);&#10;        assert_eq!(&quot;End!&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_multiple_emoji_sequence() {&#10;        let mut text = Text::new(&quot;&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 4 });&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(4, lines[0].width);&#10;        assert_eq!(&quot;&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[2].width);&#10;        assert_eq!(&quot;&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_emoji_with_text_tight() {&#10;        let mut text = Text::new(&quot;Hithereworld!&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 6 });&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(4, lines[0].width);&#10;        assert_eq!(&quot;Hi&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(6, lines[1].width);&#10;        assert_eq!(&quot;there&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(6, lines[2].width);&#10;        assert_eq!(&quot;world&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_complex_emoji_text() {&#10;        let mut text = Text::new(&quot;‍‍‍\nCodeFast&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 7 });&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(6, lines[0].width);&#10;        assert_eq!(&quot;‍‍‍&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(6, lines[1].width);&#10;        assert_eq!(&quot;Code&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(6, lines[2].width);&#10;        assert_eq!(&quot;Fast&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_single_emoji_per_line() {&#10;        let mut text = Text::new(&quot;&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 2 });&#10;        assert_eq!(4, lines.len());&#10;&#10;        assert_eq!(2, lines[0].width);&#10;        assert_eq!(&quot;&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[2].width);&#10;        assert_eq!(&quot;&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(2, lines[3].width);&#10;        assert_eq!(&quot;&quot;, lines[3].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_emoji_with_cr_lf() {&#10;        let mut text = Text::new(&quot;Fun\r\nTime⏰\rNow&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 5 });&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(5, lines[0].width);&#10;        assert_eq!(&quot;Fun&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[1].width);&#10;        assert_eq!(&quot;Time⏰&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(5, lines[2].width);&#10;        assert_eq!(&quot;Now&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_long_emoji_text_mix() {&#10;        let mut text = Text::new(&quot;Programming‍isawesometoday!&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 9 });&#10;        assert_eq!(3, lines.len());&#10;&#10;        assert_eq!(9, lines[0].width);&#10;        assert_eq!(&quot;Programming‍&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(4, lines[1].width);&#10;        assert_eq!(&quot;is&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(9, lines[2].width);&#10;        assert_eq!(&quot;awesome&quot;, lines[2].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_emoji_trailing_newlines() {&#10;        let mut text = Text::new(&quot;Hello\n\nParty\n&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 10 });&#10;        assert_eq!(4, lines.len());&#10;&#10;        assert_eq!(7, lines[0].width);&#10;        assert_eq!(&quot;Hello&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(0, lines[1].width);&#10;        assert_eq!(&quot;&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(7, lines[2].width);&#10;        assert_eq!(&quot;Party&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(0, lines[3].width);&#10;        assert_eq!(&quot;&quot;, lines[3].render(&amp;text.content));&#10;    }&#10;&#10;    #[test]&#10;    fn test_text_widget_mixed_unicode_emoji() {&#10;        let mut text = Text::new(&quot;你好worldこんにちは!&quot;);&#10;        let lines = text.wrap_text(Constraints { max_width: 8 });&#10;        assert_eq!(4, lines.len());&#10;&#10;        assert_eq!(6, lines[0].width);&#10;        assert_eq!(&quot;你好&quot;, lines[0].render(&amp;text.content));&#10;&#10;        assert_eq!(7, lines[1].width);&#10;        assert_eq!(&quot;world&quot;, lines[1].render(&amp;text.content));&#10;&#10;        assert_eq!(8, lines[2].width);&#10;        assert_eq!(&quot;こんにちは&quot;, lines[2].render(&amp;text.content));&#10;&#10;        assert_eq!(3, lines[3].width);&#10;        assert_eq!(&quot;!&quot;, lines[3].render(&amp;text.content));&#10;    }&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>